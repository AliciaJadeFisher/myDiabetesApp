<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="uk.ac.tees.s6040531.mydiabetesapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivities.ForumSection.ViewPostsActivity"></activity>
        <activity android:name=".MainActivities.ForumSection.CreatePostActivity" />
        <activity android:name=".MainActivities.ForumSection.CreateThreadActivity" />
        <activity
            android:name=".Registration_Login.AccountSetupActivity"
            android:label="@string/title_activity_account_setup"
            android:theme="@style/noActionbar" />
        <activity android:name=".Registration_Login.ResetPasswordActivity" />
        <activity
            android:name=".MainActivities.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".Registration_Login.RegistrationActivity"
            android:theme="@style/noActionbar" />
        <activity
            android:name=".Registration_Login.LoginActivity"
            android:theme="@style/noActionbar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/noActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>